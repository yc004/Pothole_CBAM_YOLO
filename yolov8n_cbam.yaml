# Ultralytics YOLOv8n-CBAM 模型配置文件
# 创新点：在 Backbone 主干网络的 SPPF 层之前添加 CBAM 注意力机制

# 1. 参数设置
nc: 1  # 类别数量 (会被 pothole_config.yaml 覆盖，但写 1 比较规范)
scales: # 模型缩放比例 [depth, width, max_channels]
  n: [0.33, 0.25, 1024]  # YOLOv8n 标准比例

# 2. Backbone 主干网络
# [from, repeats, module, args]
backbone:
  - [-1, 1, Conv, [64, 3, 2]]  # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4
  - [-1, 3, C2f, [128, True]]
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  - [-1, 6, C2f, [256, True]]
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  - [-1, 6, C2f, [512, True]]
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 3, C2f, [1024, True]]  # 8

  # ==============================================
  # 🌟 修正后的创新点配置
  # 参数解读: [1024, 7]
  # 1024: 告诉 YOLO 这一层的输出通道数保持 1024 (c2)
  # 7: 传递给 CBAM 的 kernel_size
  # ==============================================
  - [-1, 1, CBAM, [1024, 7]]  # 9

  - [-1, 1, SPPF, [1024, 5]]  # 10

# 3. Head 检测头
head:
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]
  - [[-1, 6], 1, Concat, [1]]  # cat backbone P4
  - [-1, 3, C2f, [512]] # 13

  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]
  - [[-1, 4], 1, Concat, [1]]  # cat backbone P3
  - [-1, 3, C2f, [256]] # 16 (P3/8-small)

  - [-1, 1, Conv, [256, 3, 2]]
  - [[-1, 13], 1, Concat, [1]] # cat head P4
  - [-1, 3, C2f, [512]] # 19 (P4/16-medium)

  - [-1, 1, Conv, [512, 3, 2]]
  # ⚠️ 注意下面这一行：
  # 原版是 concat 9 (SPPF)，因为我们插入了一层，SPPF 变成了 10
  # 所以这里必须改为 10，否则会报错 "Dimension mismatch"
  - [[-1, 10], 1, Concat, [1]] # cat head P5
  - [-1, 3, C2f, [1024]] # 22 (P5/32-large)

  - [[16, 19, 22], 1, Detect, [nc]] # Detect(P3, P4, P5)