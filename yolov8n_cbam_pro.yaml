# Ultralytics YOLOv8n-CBAM-PRO 模型配置文件
# 创新点 1: 多尺度部署 (Backbone + Head P3/P4/P5)
# 创新点 2: 参数调优 (Ratio=8, 保留更多通道信息)

nc: 1  # 类别数量
scales: # 模型缩放比例 [depth, width, max_channels]
  n: [0.33, 0.25, 1024]

# Backbone 主干网络
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]]  # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4
  - [-1, 3, C2f, [128, True]]
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  - [-1, 6, C2f, [256, True]]
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  - [-1, 6, C2f, [512, True]]
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 3, C2f, [1024, True]]  # 8
  
  # 🌟 CBAM 优化 (Backbone): kernel=7, ratio=8
  - [-1, 1, CBAM, [1024, 7, 8]]  # 9
  
  - [-1, 1, SPPF, [1024, 5]]  # 10

# Head 检测头
head:
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]
  - [[-1, 6], 1, Concat, [1]]  # cat backbone P4
  - [-1, 3, C2f, [512]] # 13

  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]
  - [[-1, 4], 1, Concat, [1]]  # cat backbone P3
  - [-1, 3, C2f, [256]] # 16 (P3/8-small)
  
  # 🌟 新增 Head P3 CBAM (优化小目标)
  - [-1, 1, CBAM, [256, 7, 8]] # 17

  - [-1, 1, Conv, [256, 3, 2]]
  - [[-1, 13], 1, Concat, [1]] # cat head P4 (指向第13层)
  - [-1, 3, C2f, [512]] # 20 (P4/16-medium)
  
  # 🌟 新增 Head P4 CBAM (优化中等目标)
  - [-1, 1, CBAM, [512, 7, 8]] # 21

  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 10], 1, Concat, [1]] # cat head P5 (指向SPPF层)
  - [-1, 3, C2f, [1024]] # 24 (P5/32-large)
  
  # 🌟 新增 Head P5 CBAM (优化大目标)
  - [-1, 1, CBAM, [1024, 7, 8]] # 25

  # Detect 输入改为 CBAM 后的层: 17(P3), 21(P4), 25(P5)
  - [[17, 21, 25], 1, Detect, [nc]]